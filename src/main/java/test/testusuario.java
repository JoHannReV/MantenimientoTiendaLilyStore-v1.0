package test;

import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import daoImp.loginDao;
import model.Empleado;
import model.EmpleadoDTO;
import model.Usuariosacceso;

public class testusuario {

   

	
	public static void main(String[] args) {

	/*	loginDao logindao = new loginDao();
		
	
		String username = "pedror";
        String password = "password303";
		
        Usuariosacceso usuario = logindao.AccesoUsuario(username, password);

        // Verificar y mostrar el resultado en la consola
        if (usuario != null) {
            System.out.println("Usuario autenticado con éxito: " + usuario.getUsername());
        } else {
            System.out.println("Credenciales incorrectas o error al ejecutar el procedimiento.");
      }*/
	}


        
	
	


}
