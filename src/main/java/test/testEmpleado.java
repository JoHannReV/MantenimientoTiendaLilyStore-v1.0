package test;
import model.Cargo;

import model.Empleado;
import model.Usuariosacceso;
import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;
import java.math.BigDecimal;
import java.util.List;

import daoImp.empleadoDao;
import interfaces.iEmpleado;;

public class testEmpleado {

	public static void main(String[] args) {
	
		
		/*
	        empleadoDao dao = new empleadoDao(); // Cambia esto si tu constructor es diferente
	        List<Empleado> empleados = dao.listarEmpleadoVistaPrevia();

	        // Comprobar si la lista no está vacía
	        if (empleados.isEmpty()) {
	            System.out.println("No se encontraron empleados.");
	        } else {
	            // Imprimir los resultados
	            for (Empleado empleado : empleados) {
	                System.out.println("ID: " + empleado.getCodigoEmp() +
	                                   ", Nombre: " + empleado.getNombreEmp() +
	                                   ", Apellido: " + empleado.getApellidoEmp() +
	                                   ", Tipo Documento: " + empleado.getTipoDocumento() +
	                                   ", Documento: " + empleado.getDocumentoEmp());
	                // Si no necesitas mostrar el cargo, puedes omitir esta línea
	                // System.out.println(", Cargo: " + empleado.getCargo().getNombreCargo());
	            }
	        }
		
		*/
		empleadoDao empleadoService = new empleadoDao(); // Instancia de la clase que contiene el método

      
		
		  
		
		
		
		
		
		
		
	}
	

}

	
	
	


